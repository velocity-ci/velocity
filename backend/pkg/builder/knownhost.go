package builder

import "github.com/velocity-ci/velocity/backend/pkg/domain/builder"

func updateKnownHosts(c *builder.KnownHostCtrl) {
	// fM := knownhost.NewFileManager()
	// fM.Clear()
	// for _, k := range c.KnownHosts {
	// 	fM.Save(k)
	// }
}
